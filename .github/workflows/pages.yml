name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Build dashboard
        run: |
          cp index-fixed.html index.html
          curl -L --fail --silent --show-error "https://raw.githubusercontent.com/dekipermanarg-max/dashboard-okr-bac-2627/5ea48777dfe38ab8531081434d9977adc1f397a6/data.js" -o okr-data.js
          cat >> okr-data.js <<'EOF'
          decodeBundle().then(d=>window.__OKR_DATA=d).catch(e=>console.error('OKR data bootstrap failed',e));
          EOF
          python3 - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text()
          old='<script src="data.js"></script>'
          new='<script src="okr-data.js"></script><script src="boot.js"></script>'
          if old not in s:
              raise SystemExit('Expected data.js script tag not found')
          p.write_text(s.replace(old,new))
          PY
          test -s okr-data.js
          wc -c okr-data.js
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
